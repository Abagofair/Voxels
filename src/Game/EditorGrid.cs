namespace Game
{
    internal class EditorGrid : Renderable
    {
        public EditorGrid(
            int id,
            Material material, 
            Model model) 
            : base(id, material, model)
        {
        }
    }
}
